export const environment = {
  production: true,
  // CI replaces __API_BASE_URL__ with your free API host (no trailing slash), e.g. https://hardwarehub-api.onrender.com
  apiBaseUrl: '__API_BASE_URL__/api/v1',
};
